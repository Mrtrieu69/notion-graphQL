import React from "react";

const Note = () => {
    const note = {
        id: 12,
        content: "<strong>This is new note</strong>",
    };

    return <div>Note Details</div>;
};

export default Note;
